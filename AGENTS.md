# 毕业设计项目说明（AGENTS.md）

## 1. 项目性质与当前阶段

这是一个准备启动的硕士毕业设计项目。目前主要完成了研究方向讨论、论文框架规划、基线算法选择、数据集格式讨论和相关文献检索，尚未形成正式代码工程。

后续智能体进入本目录时，应先阅读本文件，再检查项目实际目录、数据和代码状态。不得把本文件中的“推荐方案”误认为已经完成的实验结果。

## 2. 研究方向

暂定研究主题为：

> 岸基固定摄像机条件下，基于红外与可见光（RGB-T）视频的船舶多目标跟踪。

主要场景特点：

- 岸基固定摄像机；
- 红外与可见光双模态视频；
- 多船舶目标；
- 可能存在小目标、远距离目标、遮挡、交汇、尺度变化、天气与光照变化；
- 研究重点是多目标跟踪，不单独设置目标检测算法章节。

## 3. 论文总体框架

老师当前要求第三、第四、第五章分别研究三类跟踪算法，不设置独立的检测章节。

### 第二章：理论与关键技术基础

用于介绍后续研究需要的理论，不作为主要创新章节。建议包含：

- 多目标跟踪任务与基本流程；
- 红外与可见光成像特点；
- 目标检测基础；
- 运动预测与卡尔曼滤波；
- 数据关联方法；
- 外观特征与目标重识别；
- Transformer、注意力机制和目标查询；
- 常用数据集与评价指标。

第二章的数据关联算法（如匈牙利算法、贪心匹配、IoU匹配、级联匹配）不是必须全部同时使用。它们可能是并列的关联方法，也可能在一个跟踪器中分阶段组合使用。撰写时必须结合具体基线说明关系。

### 第三章：检测后跟踪（Tracking-by-Detection）

暂定基线：**ByteTrack**。

本章核心流程：

1. 检测器逐帧输出船舶检测框；
2. 卡尔曼滤波预测已有轨迹位置；
3. 对高置信度和低置信度检测分阶段关联；
4. 完成轨迹产生、保持、丢失和删除；
5. 针对船舶场景提出改进并进行实验。

本章实验至少应包含：

- 原始ByteTrack基线实验；
- 与其他检测后跟踪方法的对比实验；
- 所提模块的消融实验；
- 遮挡、交汇、小目标等典型场景分析；
- 跟踪精度、身份保持能力和速度分析。

可研究但尚未最终确定的改进方向：

- 船舶运动预测或自适应运动模型；
- 适应岸基透视的尺度/位置约束；
- 航向或运动方向一致性约束；
- 针对低置信度船舶检测的关联策略；
- 遮挡轨迹管理。

注意：透视变化、尺度变化和航向一致性是可用于关联或运动约束的场景信息，不等同于卡尔曼滤波本身。尚未实现前，不得称为本项目已经完成的创新。

### 第四章：联合检测与跟踪（Joint Detection and Tracking）

暂定基线：**FairMOT**。

本章研究检测与身份特征的共享学习：

- 一个网络同时预测目标位置和ReID身份特征；
- 推理阶段通常仍存在卡尔曼预测、特征距离计算和显式数据关联；
- 不得因为检测与ReID联合训练，就把FairMOT称为MOTR式端到端跟踪。

本章实验建议包含：

- 原始FairMOT基线实验；
- 与第三章ByteTrack的公平比较；
- 检测分支、ReID分支或特征融合模块消融；
- 红外、可见光和RGB-T融合的对比；
- 遮挡情况下身份保持与ID切换分析；
- 精度、速度和参数量分析。

可研究但尚未最终确定的方向：

- RGB-T共享/独立特征提取；
- 跨模态注意力或自适应融合；
- 对船舶ReID不稳定问题的特征增强；
- 检测与身份任务之间的损失平衡。

### 第五章：基于轨迹查询的端到端多目标跟踪

暂定基线：**MOTR**。

本章所称“端到端多目标跟踪”应采用严格定义：

- 使用检测查询发现新目标；
- 使用轨迹查询表示已有目标；
- 轨迹查询在相邻帧之间传播和更新；
- 由网络直接维持目标身份与轨迹状态；
- 弱化或取消传统手工设计的卡尔曼滤波＋IoU/ReID匈牙利匹配流水线。

本章实验建议包含：

- 原始MOTR基线实验；
- 与第三章ByteTrack、第四章FairMOT的统一对比；
- 查询更新、双模态融合或遮挡保持模块消融；
- 不同序列长度、输入分辨率和训练设置实验；
- 小目标、遮挡、交汇场景可视化；
- HOTA、IDF1、MOTA、AssA、DetA、ID切换和速度等指标分析。

可研究但尚未最终确定的方向：

- RGB-T特征进入Transformer的融合方式；
- 跨模态轨迹查询或模态自适应查询；
- 遮挡船舶轨迹查询保持；
- 船舶运动信息辅助查询更新；
- 面向小目标和远距离船舶的多尺度特征增强。

仅把MOTR应用到船舶数据上通常不足以构成强创新，至少需要围绕RGB-T融合、遮挡轨迹保持、船舶运动约束或小目标增强形成可验证的改进。

## 4. 三类跟踪算法的严格区分

后续讨论、代码注释和论文写作必须遵循以下分类：

| 类型 | 代表基线 | 检测与跟踪关系 | 是否通常有显式关联 |
|---|---|---|---|
| 检测后跟踪 | ByteTrack、DeepSORT | 先检测，再关联成轨迹 | 是 |
| 联合检测跟踪 | FairMOT、JDE | 联合学习检测和身份特征 | 通常是 |
| 轨迹查询式端到端跟踪 | MOTR、TrackFormer | 查询跨帧传播并直接维护轨迹 | 弱化或取消传统手工关联 |

以下情况不得直接称为MOTR式端到端跟踪：

- YOLO＋DeepSORT；
- YOLO＋ByteTrack；
- 仅检测器实现端到端训练；
- FairMOT/JDE仅因联合训练而被泛称“端到端”；
- 普通单目标跟踪；
- 摘要中出现“端到端”，但实际仍完全依赖检测后显式匹配。

## 5. 数据集与标注约定

计划使用CVAT进行数据标注。现阶段建议：

- 保存原始红外与可见光图像/视频；
- 保存CVAT项目或任务备份，用于以后重新导出；
- 多目标框跟踪优先导出 **MOT 1.0 / MOTChallenge格式**；
- 如基线训练需要，再转换为COCO或项目专用格式；
- 不选择MOTS PNG，除非研究任务明确包含实例分割掩膜；
- 红外和可见光帧必须做好时间同步和空间配准记录；
- 原始数据、标注、划分文件和转换后数据应分开保存；
- 训练集、验证集、测试集应按视频序列划分，避免相邻帧泄漏到不同集合。

数据量尚未最终确定。估算规模时，应根据视频时长、帧率、目标密度、场景多样性、标注成本和模型训练需求综合决定，不能只按图片张数机械判断。

建议保留的数据格式层次：

```text
data/
  raw/                 # 原始视频或图像，只读
  cvat_backup/         # CVAT备份
  annotations_mot/     # MOT格式标注
  annotations_coco/    # 需要时转换生成
  splits/              # 训练/验证/测试序列划分
```

## 6. 检测器与公平比较原则

- 检测器是否使用公开权重直接推理、在船舶数据上微调或重新训练，尚需根据数据量和实验目的确定；
- 船舶属于领域数据，通常应至少进行领域微调，并同时保留公开权重直接推理作为对照；
- 第三章和第四章的网络结构不同，不一定能物理上共用同一个检测头或权重；
- 跨章节比较时，应尽量控制训练数据、输入分辨率、评价集和阈值设置，并明确检测能力差异对结果的影响；
- 不得为了让某一章结果更好而不公开地更换测试集或评价设置。

## 7. 推荐评价指标与实验记录

主要跟踪指标建议包括：

- HOTA；
- DetA；
- AssA；
- MOTA；
- IDF1；
- ID Switches；
- FP、FN；
- FPS或单帧推理时间；
- 参数量和显存占用（必要时）。

每次正式实验至少记录：

- 章节和算法名称；
- Git提交或代码版本；
- 数据集版本与划分；
- 配置文件；
- 预训练权重来源；
- 随机种子；
- GPU、CUDA、PyTorch环境；
- 训练日志与最终权重；
- 评价命令和原始评价结果。

不得只保存最终表格而丢失原始日志。

## 8. 已确认的重要参考文献

### 国内海上多目标跟踪学位论文

1. 贺金夯：《基于红外和可见光图像的多船舶融合跟踪》  
   当前本机文件：`C:\Users\cao\Downloads\基于红外和可见光图像的多船舶融合跟踪_贺金夯.pdf`

2. 单雄飞：《基于计算机视觉的海上多目标船舶检测与跟踪关键技术研究》  
   当前本机文件：`C:\Users\cao\Downloads\基于计算机视觉的海上多目标船舶检测与跟踪关键技术研究_单雄飞.pdf`

这两篇是海上场景的重要参考，但不能自动归类为MOTR式端到端跟踪。

### 国内轨迹查询式端到端MOT学位论文

沈楷传：《空地协同无人系统跨视角多目标跟踪》，东南大学，2025年硕士论文。

- DOI：<https://doi.org/10.27014/d.cnki.gdnau.2025.002324>
- 论文第二章提出端到端式多目标跟踪算法OAMATR；
- 目录明确包含可变形跨帧注意力、遮挡预测网络和可变轨迹查询；
- 其第三章仍包含目标检测、运动优化和显式数据关联，因此应按章节判断范式，不能笼统地把整篇所有方法都称为端到端。

### MOTR原始工作

- 论文：<https://arxiv.org/abs/2105.03247>
- 正式出版：<https://link.springer.com/chapter/10.1007/978-3-031-19812-0_38>
- 官方代码：<https://github.com/megvii-research/MOTR>

MOTR由中国研究团队提出，但原始文献是会议论文，不是硕博学位论文。

## 9. 文献检索与引用规则

用户明确要求：总结文献时必须提供可访问的原文、DOI、出版社页面、学位库记录或高校机构库链接。

后续智能体必须遵守：

- 不得编造作者、学校、年份、题名、DOI或下载链接；
- 优先核对论文原文、目录、摘要和方法流程；
- 区分学位论文、期刊论文、会议论文和博客解读；
- 知网或万方只能访问记录页时，应明确说明全文可能需要机构权限；
- 判断“端到端”时必须检查是否存在轨迹查询跨帧传播或是否仍依赖显式数据关联；
- 找不到完全匹配的论文时，应写“暂未检索到可核实记录”，不得断言“中国没有”；
- 海事雷达、AIS跟踪与视觉RGB-T多目标跟踪应分别归类。

## 10. 当前尚未确定的问题

以下内容仍属于待决策事项，不得擅自写成最终方案：

- 论文正式题目；
- 第三、第四、第五章各自最终创新点；
- RGB-T融合发生在输入、特征、决策还是查询层；
- 使用哪些公开数据集及自建数据集规模；
- 检测器型号与训练策略；
- 具体硬件能否完成MOTR端到端训练；
- 是否采用MOTRv2、TrackFormer或其他方法作为第五章补充对比；
- 各章最终章节编号和标题。

遇到这些问题时，智能体应先提供选项、代价和推荐方案，再由用户确认；不得基于旧对话自行认定已经定稿。

## 11. 建议的项目目录

正式建立工程时可参考：

```text
thesis-tracking/
  AGENTS.md
  README.md
  docs/
    PROJECT_MEMORY.md
    literature/
    thesis_outline.md
  data/
  third_party/
    ByteTrack/
    FairMOT/
    MOTR/
  src/
    common/
    chapter3_tbd/
    chapter4_jdt/
    chapter5_e2e/
  configs/
  scripts/
  tests/
  experiments/
  outputs/
```

第三方基线仓库尽量保持原始状态；项目改进代码、配置和适配层优先放在本项目目录中。若必须修改第三方代码，应记录补丁和上游版本。

## 12. 智能体工作规则

- 默认使用中文与用户交流；
- 回答时区分“已经确认”“建议方案”“待验证假设”；
- 修改代码前先判断属于第三、第四还是第五章；
- 优先做可复现的小规模验证，再开展完整训练；
- 不得删除原始数据、已有实验、论文或用户文件；
- 任何清理、覆盖、批量移动和重新划分数据集操作前，先核对目标并取得明确授权；
- 工作区可能包含用户已有改动，应保留无关改动；
- 新增脚本应提供用途、输入、输出和运行方法；
- 训练或评估失败时保留错误日志，并说明失败原因；
- 不能仅以指标提升宣称创新，应给出问题动机、方法变化、消融实验和场景解释；
- 不应为了迎合既定章节而错误归类算法。

## 13. 下一步建议

当前最合理的启动顺序是：

1. 创建正式项目目录和版本库；
2. 记录电脑GPU、显存、CUDA和Python环境；
3. 整理原始RGB-T视频并确认同步/配准状态；
4. 设计CVAT标注规范和数据集划分；
5. 先跑通ByteTrack公开基线和评价工具；
6. 再依次跑通FairMOT与MOTR；
7. 建立统一的实验记录和评价流程；
8. 在基线结果和失败案例基础上确定每章创新点。

